baseURL: "https://yanomic.com/"
title: "Yanomic"
languageCode: "en-us"

params:
  description: "Yanomic.com"
  authorName: "Yan Hai"
  copyrightYearStart: 2023
  # Local `hugo server` — override in config/production for deployed builds
  analyticsEndpoint: http://localhost:3101/analytics/page-visits

# menu:
  # main:
    # - name: Blogs
      # url: /blogs/
      # weight: 20

module:
  imports:
    - path: "github.com/yanomic/yanomic-theme"
markup:
  goldmark:
    renderer:
      unsafe: false
