# Merged on top of config/_default when HUGO_ENVIRONMENT=production.
# Only list keys that differ from _default — no need to copy the whole file.

params:
  analyticsEndpoint: "http://api.yanomic.com:3101/analytics/page-visits"
